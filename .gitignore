# dependencies / build output
node_modules/
dist/
.vite/
*.tsbuildinfo

# rust / tauri
src-tauri/target/
src-tauri/gen/
# staged helper binary (rebuilt by scripts/stage-helper.mjs at bundle time)
src-tauri/binaries/

# reference clones (balena etcher + rpi-imager) — reference only, not part of Pyro
upstream/

# local harness state
.claude/
CLAUDE.local.md

# misc
*.log
.DS_Store
.env

# rust workspace build output (root)
/target/
